# DESIGN.md — BillGen Client Design Reference

> Auto-generated by the design-auditor skill. Update this file when design conventions change.
> Scope: `src` | Generated: 2026-05-21

---

## 1. Overview

BillGen's client code is a focused, utility-first UI scaffold that leans on Tailwind for visual styling. The aesthetic is functional and minimal with a slightly airy density (generous paddings and gaps) and a neutral, professional tone suitable for admin/workspace interfaces.

---

## 2. Tech Stack & Style Approach

| Concern       | Approach                          |
|---------------|-----------------------------------|
| Framework     | React (Next.js app directory)     |
| Styling       | Tailwind CSS utility classes + a small globals.css |
| Tokens        | No dedicated token file found in src (Tailwind classes used inline). Check repo root for tailwind.config.js |
| Icons         | Inline SVGs in-place (no external icon lib detected in src) |
| Animation     | Tailwind transition utilities (transition-colors, transition-shadow, focus:ring) |

---

## 3. Color Palette

### Primitive Colors

Colors are expressed via Tailwind semantic classes (e.g., slate-900, slate-600, blue-600, amber-500, gray-50, white) and some arbitrary values (radial-gradient used on the home page). No CSS custom properties for color were found under src.

### Semantic Colors

| Role     | Token / Value      | Usage                     |
|----------|--------------------|---------------------------|
| Primary  | blue-600 / blue-700 | Primary CTA buttons (bg-blue-600, hover:bg-blue-700) |
| Accent   | amber-500 / amber-400 | Accent action (Import Data) |
| Success  | green-600 / green-100 | Illustrative card/icon backgrounds |
| Warning  | (none explicit)    | -                         |
| Error    | red-500 / red-600  | Form validation and error messages (text-red-600, bg-red-50) |
| Neutral  | slate-900 / slate-600 / slate-500 / slate-200 | Headings, body text, borders, subdued text |

---

## 4. Typography

| Role        | Font Family | Size / Example class | Weight / Note | Line Height |
|-------------|-------------|----------------------|---------------|-------------|
| Body        | Inter, system-sans (declared in globals.css) | text-base / text-lg used in places | normal / semibold for headings | default Tailwind line-heights used |
| Heading 1   | Inter / system stack | text-4xl / sm:text-6xl (home) | font-semibold | - |
| Heading 2   | Inter | text-2xl / text-3xl | font-bold / font-semibold | - |
| Small/Label | Inter | text-sm | font-medium | - |
| Code        | Not detected (no monospace token found in src) | - | - | - |

Base font-family is set in src/app/globals.css: Inter plus system fallbacks.

---

## 5. Spacing Scale

Spacing is handled with Tailwind utility classes (px-4, py-2, p-6, p-8, space-y-6, gap-10, max-w-*). The codebase uses medium-to-spacious values by default (p-6 / p-8 commonly used for cards and panels).

---

## 6. Layout Conventions

- **Grid system**: Utility-first responsive layout using flex + CSS Grid utilities (e.g., md:grid-cols-2, grid gap-4).
- **Container max-width**: max-w-5xl / max-w-7xl used in top-level pages.
- **Responsive breakpoints**: Tailwind breakpoints (sm, md, lg, etc.) are used (e.g., sm:text-6xl, md:grid-cols-2).
- **Common padding patterns**: px-6 py-16 for page sections, p-6/p-8 for cards, max-w-2xl / max-w-3xl used for readable content widths.

---

## 7. Component Conventions

### Buttons
- Implemented inline with Tailwind classes: primary uses bg-blue-600 with hover:bg-blue-700 and focus:ring utilities. Disabled state uses bg-gray-400 and cursor-not-allowed.
- Rounded corners via rounded-md / rounded-lg.

### Cards / Panels
- Background: white or white/opacity (bg-white/80), border: border-slate-200, shadow: shadow-sm / shadow / shadow-md, padding: p-5 / p-6 / p-8, radius: rounded-lg / rounded-2xl.

### Form Inputs
- Inputs use w-full, px-4 py-2, border, rounded-md and focus:ring-2 focus:ring-blue-500. Error state uses border-red-500 and helper text text-red-600.
- Accessibility: aria-invalid and aria-describedby are used in forms (good practice visible in OnboardingForm).

### Badges / Tags
- Small uppercase labels use text-sm font-semibold tracking-[...] and text-slate-500 for subtle badges.

### Other
- Pages and feature blocks are implemented directly as React components (pages and client components for forms). There is no separate component library in src — components live inline in pages or dedicated small components directory is empty (.gitkeep).

---

## 8. Motion & Animation

| Property        | Value(s)                   |
|-----------------|----------------------------|
| Transition speed| Default Tailwind speeds (utilities like transition-colors used) |
| Easing          | Tailwind defaults (no custom easing detected in src) |
| Keyframes       | None found in src (no named @keyframes detected)

---

## 9. Decorative Conventions

- **Backgrounds**: Uses Tailwind utilities and an explicit radial + linear gradient on the homepage via an arbitrary bg-[...] class.
- **Dividers**: Subtle borders (border-slate-200) with opacity; no separate divider component detected.
- **Shadows**: shadow-sm / shadow / shadow-lg used; values come from Tailwind configuration.
- **Border radius**: rounded-md, rounded-lg, rounded-2xl are used consistently.
- **Icon usage**: Inline SVGs embedded in pages (no external icon package referenced in src).

---

## 10. Naming Conventions

- **CSS classes**: Utility-first Tailwind classes used inline via className.
- **Component files**: PascalCase for React components and pages (e.g., OnboardingForm.tsx). Page directories follow Next.js file conventions.
- **Token names**: No token naming pattern found in src (no CSS variables for colors or tokens).

---

## 11. Do's and Don'ts

### Do
- Use Tailwind utility classes for layout, spacing, and color tokens (consistent with existing code).
- Prefer inline small SVGs for simple icons unless adding a shared icon component.

### Don't
- Introduce ad-hoc global CSS variables in src without adding a token file or documenting in tailwind.config.js.
- Mix CSS Modules or styled-components in src; the current codebase consistently uses Tailwind.

---

## 12. Open Questions / WIP

- No token file was found under src. Tailwind configuration (tailwind.config.js) and any design tokens likely live at the repository root — consider running a repo-root audit to extract canonical color and spacing scales.
- Components directory in src is empty (.gitkeep) while multiple UI fragments live inside pages; consider creating a small shared components folder for repeated patterns (Button, Card, Icon) to avoid duplication.
- No animation keyframes or explicit duration tokens found; if motion design is required, decide and centralize durations in tokens or Tailwind config.

---

> Inconsistencies flagged: No mixed styling approaches detected in src — styling is consistently Tailwind-based. Colors and tokens are expressed as Tailwind classes and arbitrary values; no CSS custom properties for colors were found in src.
